The mobile phone experience of the UI is poor. We need to plan out the UI
so it will work well on a mobile phone.

Use your knowledge of the application now, together with your frontend
superpower to plan how the UI components should change so that the user
has an engaging experience with the game's UI, that component do not 
overlap, that the map is easily navigable.

Here are my thoughts about the current experience:

 - overlapping UI components
 - horizontal directional arrows on the tiles are not visible in portrait mode
 - should support both landscape and portrait mode

Use your superpower to plan how to fix the UI. You can use google chrome to take
screenshots of the current layout if required. Use best practices in UI's for
mobile devices.

Write a self-contained HTML file that shows screenshots of how the UI will look,
and how the user will use the UI.