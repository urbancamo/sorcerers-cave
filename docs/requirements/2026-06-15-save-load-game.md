Each game should have an id. 
The id should be four uppercase letters. 

When a new game is created, allocate it a four letter ID. You should check that the ID doesn't already exist
in the database.

Add an option to save the game from within the HUD.
This should save the game state to the convex database.
It should then show the four letter ID of the game, then take the player back to the splash screen.

Add a new splash screen option to resume a saved game, associated with the single player option. This should request a four letter game id, and if it exists restore the game state as saved.

Use planning mode and write an implementation plan.
