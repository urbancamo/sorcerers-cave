# Build caches and VCS — never needed in the deployment upload.
.git
.turbo
**/.turbo
**/*.tsbuildinfo

# Source design assets / docs: the runtime assets are committed under
# apps/web/public/assets (served by Vite), so these ~160MB aren't built from.
docs

# Regenerated by the build.
apps/web/dist
